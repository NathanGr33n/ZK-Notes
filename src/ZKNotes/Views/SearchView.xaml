<UserControl x:Class="ZKNotes.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search box -->
        <DockPanel Grid.Row="0">
            <TextBlock Text="Search" FontSize="16" FontWeight="SemiBold" DockPanel.Dock="Left"
                       Foreground="{StaticResource ForegroundBrush}" VerticalAlignment="Center"
                       Margin="0,0,12,0"/>
            <TextBox Text="{Binding Search.Query, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="14" Padding="8,6"
                     Background="{StaticResource SurfaceBrush}"
                     Foreground="{StaticResource ForegroundBrush}"
                     CaretBrush="{StaticResource ForegroundBrush}"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
        </DockPanel>

        <!-- Tag filters -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Search.AllTags}" Margin="0,8,0,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding StringFormat='#{0}'}"
                            Click="Tag_Click" Tag="{Binding}"
                            Padding="6,2" Margin="2"
                            FontSize="11" Cursor="Hand"
                            Background="{StaticResource SurfaceBrush}"
                            Foreground="{StaticResource AccentBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Results -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Search.Results}"
                 Background="Transparent" BorderThickness="0"
                 Foreground="{StaticResource ForegroundBrush}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="Result_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,8">
                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}"/>
                        <TextBlock Text="{Binding Snippet}" FontSize="12"
                                   Foreground="{StaticResource SecondaryForeground}"
                                   TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                        <TextBlock FontSize="10" Foreground="{StaticResource AccentBrush}" Margin="0,2,0,0">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} · {1:MMM dd yyyy}">
                                    <Binding Path="Id"/>
                                    <Binding Path="LastEdit"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="Margin" Value="0,1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="Transparent"
                                        Padding="{TemplateBinding Padding}" CornerRadius="4">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#2a2d2e"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
